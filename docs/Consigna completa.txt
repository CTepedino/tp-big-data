# Big Data

## Proyecto de curso
### Primer Cuatrimestre - 2026

# Cloud Provider Analytics
## (ETL + Streaming + Serving en Cassandra)

## Contexto

Eres el equipo de datos de un proveedor de nube. Tu misión es ingestar, limpiar, conformar y publicar datos de clientes para analítica de FinOps, Soporte y Producto.

La organización exige dos capacidades:

1. Near real-time para métricas operativas (uso/consumo, costos incrementales).
2. Batch diario/mensual para maestros (CRM) y facturación.

Los datos aterrizan crudos con inconsistencias, nulos, tipos ambiguos, anomalías y una evolución de esquema a mitad del histórico (aparecen campos nuevos como `carbon_kg` y `genai_tokens`).

Tu objetivo: construir un pipeline con PySpark en Google Colab, usando Parquet como storage intermedio gestionado por Spark y publicar marts de analítica en AstraDB (Cassandra) para ser consultados por herramientas de visualización (Tableau, Power BI, Superset, Grafana, etc.).

---

# Datos provistos (Landing)

El zip incluye (carpeta `datalake/landing/`):

- `customers_orgs.csv`: clientes/tenants (industria, región, plan, NPS… con nulos y valores ruidosos).
- `users.csv`: usuarios por organización (roles, actividad, timestamps).
- `resources.csv`: recursos cloud por servicio (compute, storage, database, networking, analytics, genai).
- `support_tickets.csv`: tickets (categoría, severidad, SLA, CSAT con nulos, abiertos/cerrados).
- `marketing_touches.csv`: toques de marketing (canales, conversiones).
- `nps_surveys.csv`: NPS temporales por organización (con nulos).
- `billing_monthly.csv`: facturación 3 meses (impuestos, créditos/ajustes, monedas).
- `usage_events_stream/*.jsonl`: feed de eventos de uso (para Structured Streaming).

### Evolución de esquema

A partir de aproximadamente 45 días atrás aparece `schema_version=2` con:

- `carbon_kg`
- Para GenAI: `genai_tokens`

También existen:

- Campos con nulos.
- Valores atípicos (por ejemplo `cost_usd_increment` negativo ocasional o picos muy grandes).

> Nota: los archivos de eventos están intencionalmente fragmentados para simular micro-lotes.

---

# Arquitectura y Zonas del Data Lake

### Landing (Raw Inmutable)

Archivos originales. No modificar.

### Bronze (Raw Estándar)

Mismo grano que la fuente pero con:

- Tipificación explícita.
- Dedupe por `event_id`.
- Marcas de ingesta:
  - `ingest_ts`
  - `source_file`

### Silver (Conformado)

Datos limpios y conformados:

- Campos normalizados.
- Joins con dimensiones.
- Tratamiento de nulos y outliers.
- Control de versiones de esquema.
- SCD si aplica.

### Gold (Marts de negocio)

Vistas/tablas orientadas a analítica:

- FinOps
- Soporte
- Producto / Usage

Listas para servir en Cassandra (AstraDB).

### Formato intermedio

Parquet particionado (por ejemplo `date=` y/o `service=`).

### Control de calidad

Reglas de validación (propias o con Expectations) para bloquear o aislar datos incorrectos.

---

# Patrón arquitectónico (elige y justifica)

## Lambda

### Batch

- Maestros (orgs, users, resources).
- Facturación.
- Encuestas (NPS).

### Streaming

- `usage_events_stream`
- Ventanas.
- Watermarks.
- Upserts idempotentes.

## Kappa

Trata todo como stream, alimentando las capas Silver/Gold mediante vistas derivadas (re-stream/backfill desde archivos).

### Requisito

Implementar al menos:

- Streaming de eventos.
- Batch de maestros/facturación.

Explicar por qué la elección realizada es adecuada.

---

# Requerimientos técnicos (obligatorios)

## 1. Ingesta

### Batch

Leer CSV/JSON desde `landing/` a Bronze Parquet particionado.

### Streaming

Structured Streaming:

- Fuente JSON por directorio.
- Esquema explícito.
- `withWatermark`.
- Deduplicación por `event_id`.
- Manejo de late data.

---

## 2. Calidad de datos

### Tipos consistentes

Ejemplo:

- `value` puede venir como string → castear con fallback.

### Reglas mínimas

- `event_id` no nulo y único por ventana.
- `cost_usd_increment ∈ [-0.01, +∞)`.
- Flag de anomalía si supera `p99 * X`.
- `unit` no nulo cuando `value` no nulo (o imputación).
- Compatibilización entre `schema_version` v1 y v2.

### Quarantine

Registrar errores en Parquet aparte.

---

## 3. Transformaciones (Silver)

### Normalización

- Números.
- Fechas.
- Regiones.
- Servicios.

### Enriquecimiento

Join con:

- Orgs.
- Users.
- Resources.

### Features calculadas

- `daily_cost_usd` por organización y servicio.
- `requests`.
- `cpu_hours`.
- `storage_gb_hours`.
- `genai_tokens`.
- `carbon_kg`.

### Anomalías

Flags utilizando 3 métodos a elección:

- Z-score.
- MAD.
- Percentiles (p-tiles).

---

## 4. Gold (marts)

### FinOps

#### org_daily_usage_by_service

```
(org_id, usage_date, service)
```

Métricas y costos.

#### revenue_by_org_month

```
(org_id, month)
```

- Revenue USD.
- Créditos.
- Impuestos.
- FX aplicado.

#### cost_anomaly_mart

```
(org_id, date, service)
```

- Score.
- Flag.

---

### Soporte

#### tickets_by_org_date

```
(org_id, date, severidad)
```

- Counts.
- SLA breach rate.
- CSAT promedio.

---

### Producto / Usage (GenAI)

#### genai_tokens_by_org_date

```
(org_id, date)
```

- Total tokens.
- Costo estimado.

---

## 5. Serving en AstraDB (Cassandra)

- Crear keyspace (por ejemplo `cloud_analytics`).
- Diseñar tablas por consulta (query-first modeling).
- Cargar Gold a Cassandra usando:
  - Spark Connector.
  - `foreachBatch` + driver Python.

---

## 6. Idempotencia

Permitir reprocesamiento sin duplicados:

- Checkpointing.
- Claves naturales.
- Upserts.

---

## 7. Performance

- Particionado adecuado.
- Reparquet si es necesario.
- `coalesce()`.
- `repartition()`.

---

## 8. Documentación

Incluir:

- Diagrama de arquitectura.
- Diccionario de datos.
- Decisiones y trade-offs.

Ejemplos:

- Particionamiento.
- Claves Cassandra.
- Umbrales de anomalías.

---

## 9. Demo

Ejecutar 5 consultas típicas sobre AstraDB que respalden dashboards.

---

# Consultas mínimas que deben responderse (desde AstraDB)

## 1.

Costos y requests diarios por organización y servicio en un rango de fechas.

## 2.

Top-N servicios por costo acumulado en los últimos 14 días para una organización.

## 3.

Evolución de tickets críticos y tasa de SLA breach por día (últimos 30 días).

## 4.

Revenue mensual con créditos e impuestos aplicados (normalizado a USD).

## 5.

Tokens GenAI y costo estimado por día (si existen).

> Incluir los CQL y capturas de resultados en el notebook.

---

# Entregables

## 1. Presentación

Debe detallar:

- Arquitectura.
- Tecnologías.
- Proceso de desarrollo.
- Recursos utilizados.
- Propuesta de ingeniería para el contexto de negocio.
- Arquitectura y zonas del Data Lake.
- Patrón arquitectónico.
- Requerimientos técnicos.
- Consultas mínimas de demostración.

## 2. Video explicativo

Basado en:

- La presentación.
- La solución implementada con Spark y Cassandra.

Debe mostrar:

- Funcionamiento del pipeline.
- Resultados del ETL.

---

# Evaluación

## 1. Diseño y desarrollo

Se evaluará:

- Ingenio de la solución.
- Arquitectura.
- Implementación en Spark y Cassandra.

Todos los datos deben estar en un Keyspace de Cassandra utilizando colecciones.

## 2. Calidad

Correcto análisis de los datasets y ajuste de los datos según las reglas de negocio.

## 3. Profundidad

Decisiones tomadas y estrategias utilizadas, tanto de diseño como técnicas.

## 4. Storytelling

Se valorará la narrativa utilizada para presentar la solución en el video.